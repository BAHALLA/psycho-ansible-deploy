apiVersion: v1
kind: Service
metadata:
  name: {{ container.app_name }}
  namespace: {{ container.namespace }}
spec:
  selector:
    app: {{ container.app_name }}
  ports:
    - port: {{ service.port }}
      targetPort: {{ service.port }}
      protocol: TCP
